package com.corejava.dynamicprograming;

public class MemoizationTricks {

	/*
	 * Step 1. Make it work
	 * 	1. Visualize the problem as a tree
	 * 	2. Implement the tree using recursion
	 * 	3. Test it
	 * 
	 * Step 2: Make it efficient
	 * 	1. Add a memory object
	 * 	2. Add a base case to return memory values
	 * 	3. Store return values into the memory
	 */
}
